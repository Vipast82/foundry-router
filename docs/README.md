# docs/

Place `foundry-router-build-design.md` (the authoritative design document this
implementation was built from) in this directory — the top-level README links
to it here. It is not auto-generated into the repo to avoid drifting from your
canonical copy; every section reference in the code comments (`§4.2`, `§4.3`,
`§7`…) points at that document.
